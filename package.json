{
  "name": "catalog-engine",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "better-sqlite3": "^12.11.1",
    "kysely-codegen": "^0.20.0",
    "turbo": "^2.10.5"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
