import { Kysely } from 'kysely';
import { Dialect } from '../dialect';
import { DB } from '../outputs/postgres.output';
export declare const migrate: (dialect: Dialect, connectionString: string) => Promise<Kysely<DB>>;
export declare const addExtraColumn: (db: Kysely<DB>, dialect: Dialect) => Promise<void>;
